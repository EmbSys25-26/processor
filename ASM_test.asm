.org 0x0000
J(MAIN)

.org 0x0020
J(ISR_HANDLER)

.org 0x0100
MAIN:
CLI
STI
NOP

.word 0xDEADBEEF
.byte 0xFF
.byte 0xAA

LI(r1, 0x123)
LI(r2, 0x005)

ADD r3, r1
SUB r3, r2
AND r4, r1
XOR r5, r2
ADC r6, r1
SBC r7, r2
CMP r1, r2
SRL r1, r2
SRA r3, r4

GETCC r8
SETCC r8

RSUBI r1, #15
ANDI r2, #0xA
XORI r3, #0
ADCI r4, #5
RSBCI r5, #1
RCMPI r6, #8

ANDI r7, MASK_VAL

ADDI r8, r1, #7
ADDI r9, r2, VAL_POS

SW r1, sp, #0
LW r2, sp, #0
SB r3, sp, #1
LB r4, sp, #1

CALL(SUB_ROUTINE)

BR FORWARD_BR
NOP

FORWARD_BR:
BEQ BACKWARD_BR

BACKWARD_BR:
BC FORWARD_BR
BV FORWARD_BR
BLT FORWARD_BR
BLE FORWARD_BR
BLETU FORWARD_BR
BLEU FORWARD_BR

IMM FAR_LABEL
JAL lr, r0, FAR_LABEL

PUSH(r1)
POP(r1)
MOV(r3, r2)
SUBI(r4, r1, 2)
NEG(r5)
COM(r6)
SLL(r7)
LEA(r8, sp, 4)
OR(r9, r1)

END_LOOP:
BR END_LOOP

SUB_ROUTINE:
RET

ISR_HANDLER:
ISR_PROLOGUE
ISR_PRO
IRET
ISR_EPILOGUE

.org 0x0500
FAR_LABEL:
NOP

VAL_POS .equ 3
MASK_VAL .equ 12
